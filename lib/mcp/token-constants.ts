/** Prefix distinguishes MCP PATs from Supabase JWTs (which contain dots). */
export const MCP_TOKEN_PREFIX = "ptmcp_";

/** Default lifetime for newly issued MCP personal access tokens. */
export const MCP_TOKEN_TTL_DAYS = 30;
